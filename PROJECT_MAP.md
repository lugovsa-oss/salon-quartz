content/            — публикуемый контент
content_backup/     — архив
quartz/static/      — статика сайта
quartz/styles/      — стили
public/             — результат сборки

Сборка:
npx quartz build
npx quartz build --serve

Публикация:
GitHub → Cloudflare → alugovskoy.net